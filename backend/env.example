# =====================================================
# ENVIRONMENT CONFIGURATION
# Copy this file to .env and update with your values
# =====================================================

# Server Configuration
NODE_ENV=development
PORT=5000
API_PREFIX=/api/v1

# =====================================================
# SSL/TLS Configuration
# =====================================================
# Enable/Disable HTTPS
ENABLE_SSL=false
# Đặt true để enable HTTPS
# Development: Dùng self-signed certificate
# Production: Dùng Let's Encrypt hoặc valid certificate

# SSL Certificate Paths (relative to backend/)
SSL_KEY_PATH=./certs/localhost.key
SSL_CERT_PATH=./certs/localhost.crt
# SSL_CA_PATH=./certs/ca-bundle.crt

# Production Example (Let's Encrypt):
# SSL_KEY_PATH=/etc/letsencrypt/live/yourdomain.com/privkey.pem
# SSL_CERT_PATH=/etc/letsencrypt/live/yourdomain.com/fullchain.pem

# Tạo self-signed certificate cho development:
# cd backend/scripts
# Windows: ./generate-ssl-cert.ps1
# Linux/Mac: ./generate-ssl-cert.sh

# Database Configuration (MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=antique_store
DB_USER=root
DB_PASSWORD=your_password
DB_DIALECT=mysql

# Database Configuration (PostgreSQL) - Alternative
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=antique_store
# DB_USER=postgres
# DB_PASSWORD=your_password
# DB_DIALECT=postgres

# JWT Secret Keys
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d
JWT_REFRESH_SECRET=your-refresh-token-secret
JWT_REFRESH_EXPIRE=30d

# CORS Configuration
# Support both HTTP and HTTPS for development
CORS_ORIGIN=http://localhost:5173,https://localhost:5173
# For multiple origins: http://localhost:5173,http://localhost:3000,https://localhost:5173
# Production: https://yourdomain.com,https://www.yourdomain.com

# File Upload
MAX_FILE_SIZE=5242880
# 5MB in bytes

# Pagination
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100

# Security
BCRYPT_ROUNDS=10

# Payment Gateway (Mock for now)
VNPAY_TMN_CODE=your_vnpay_code
VNPAY_HASH_SECRET=your_vnpay_secret
MOMO_PARTNER_CODE=your_momo_code
MOMO_ACCESS_KEY=your_momo_key

# QR Code Payment (VietQR) - Giả lập
# Mã ngân hàng (BIDV = BIDV, VCB = Vietcombank, TCB = Techcombank, VPB = VPBank, etc.)
QR_BANK_ID=BIDV
# Tên ngân hàng
QR_BANK_NAME=Ngân hàng TMCP Đầu tư và Phát triển Việt Nam (BIDV)
# Số tài khoản ngân hàng
QR_ACCOUNT_NUMBER=6263828497
# Tên chủ tài khoản
QR_ACCOUNT_NAME=TRUONG HO MINH VUONG

# Email Service (Optional - for future)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Cloudinary (Optional - for image upload)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Redis (Optional - for caching)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Logging Configuration
LOG_LEVEL=info
# Levels: error, warn, info, http, debug

# Sentry Configuration (Production Error Tracking)
# Get your DSN from https://sentry.io/
SENTRY_DSN=
# Example: SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
# Leave empty to disable Sentry
SENTRY_ENVIRONMENT=production
SENTRY_TRACES_SAMPLE_RATE=1.0



